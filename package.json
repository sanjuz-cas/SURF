{
  "name": "multi-agent-system",
  "version": "1.0.0",
  "description": "A comprehensive multi-agent system with React frontend and FastAPI backend",
  "main": "index.js",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "install-all": "npm install && cd frontend && npm install",
    "dev": "concurrently \"cd backend && python main.py\" \"cd frontend && npm start\"",
    "setup": "npm run install-all && pip install -r requirements.txt"
  },
  "keywords": [
    "multi-agent",
    "langgraph",
    "fastapi",
    "react",
    "typescript",
    "postgresql"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}